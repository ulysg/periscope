using Gtk 4.0;
using Adw 1;

template $Window : Adw.ApplicationWindow {
  default-width: 600;
  default-height: 300;
  title: _("Periscope");

  Box {
  $SongGrid song_grid {
  }
  }

  Box {
  $SongPlayer song_player {
  }
  }
}