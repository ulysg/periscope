using Gtk 4.0;
using Adw 1;

template $Window : Adw.ApplicationWindow {
  default-width: 1000;
  default-height: 500;
  title: _("Periscope");

  Box {
    orientation: vertical;
    valign: end;

    $SongGrid song_grid {
    }

    $SongPlayer song_player {
    }
  }
}