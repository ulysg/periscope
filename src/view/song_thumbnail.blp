using Gtk 4.0;
using Adw 1;

template $SongThumbnail : Adw.Bin {
  Box {
    orientation: vertical;

    Picture cover {
      halign: center;
      valign: center;
      content-fit: contain;
    }

    Label title {
      wrap: true;
    }
  }
}